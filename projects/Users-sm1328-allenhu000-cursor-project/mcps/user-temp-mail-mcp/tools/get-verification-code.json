{
  "name": "get-verification-code",
  "description": "从收件箱中查找并提取验证码：拉取邮件列表，从最新一封开始检查正文中的数字验证码（默认 4–8 位），返回第一个匹配到的验证码及对应邮件信息。若尚未收到验证码邮件可稍后重试。",
  "arguments": {
    "type": "object",
    "properties": {
      "token": {
        "type": "string",
        "description": "create-inbox 返回的 token"
      },
      "codePattern": {
        "type": "string",
        "description": "可选，验证码正则，默认匹配 4–8 位数字，如 \\b(\\\\d{6})\\\\b 表示 6 位数字"
      }
    },
    "required": [
      "token"
    ],
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}