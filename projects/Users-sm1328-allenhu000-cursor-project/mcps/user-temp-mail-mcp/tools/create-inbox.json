{
  "name": "create-inbox",
  "description": "使用选定的 domain 和自定义用户名创建临时邮箱。先调用 list-domains 获取可选 domain，再传入此处。返回 address、password、token，后续拉邮件和取验证码需使用 token。",
  "arguments": {
    "type": "object",
    "properties": {
      "username": {
        "type": "string",
        "minLength": 1,
        "description": "邮箱前缀（本地部分），如 myuser，最终邮箱为 myuser@<domain>"
      },
      "domain": {
        "type": "string",
        "minLength": 1,
        "description": "域名，从 list-domains 返回的 domain 列表中选一个，如 dollicons.com"
      },
      "password": {
        "type": "string",
        "description": "可选，不传则自动生成"
      }
    },
    "required": [
      "username",
      "domain"
    ],
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}