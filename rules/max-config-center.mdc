---
description: sunmi-web/max-config-center 配置命名、v2 目录结构与钉钉功能清单对齐；禁止改 configuration_v1
alwaysApply: false
---

# max-config-center 配置范式

## 仓库与版本

- 仓库：<https://github.com/sunmi-web/max-config-center>
- **仅维护 `configuration_v2/`**；**禁止修改 `configuration_v1/`**（旧版，废弃）。

## fullKey 命名

- 唯一形式：**`<MODULE>.<KEY>`**，与 README「模块」英文缩写一致（如 `CHECKOUT.PRODUCT_ITEM_AUTO_MERGE_ENABLE`）。
- 模块列表以仓库 `README.md`「模块」节为准（如 `SYS`、`CHECKOUT`、`SETTLEMENT`、`I18N`、`STORE`、`PAYMENT`、`VOICE` 等）。
- 运行时 API 语义见 README：`enable`/`disable`（模块开关）、`get`/`set`、`pull`/`push`、`batchPull`/`batchPush`。

## v2 代码布局（改 Key 时对照）

| 层级 | 文件 | 说明 |
|------|------|------|
| 通用（跨端/云端） | `configuration_v2/config/Conf_Universal.js` | 与 `Handler_Universal.js` 成对 |
| 端本地 | `configuration_v2/config/Conf_Local.js` | 与 `Handler_Local.js` 成对；需云端同步的项必须在 Handler 中注册 |
| 聚合 | `configuration_v2/config/ConfEntry.js` | 合并 Universal + Local，一般无需改 |
| Handler | `configuration_v2/handler/Handler_*.js` | `settingKey` / `settingType`（如 `SWITCH`、`CONTENT`）或 `pull`/`push` |

- 新增配置：先判断放 **Universal** 还是 **Local**，在对应 `Conf_*.js` **该 MODULE 分段内**（段末最后一项之后）插入键与默认值，禁止堆在 `return` 或文件末尾；Handler 同理按 **同模块分段** 插入。
- **README.md**「配置一览」须在 **同 MODULE 分组行内** 插入新行，勿默认加在整张表最后导致分组断裂。

## 功能清单（钉钉 AI 表格）

- 链接与 baseId 见 README「功能清单」；字段含义：端、域、模块、Key、可选值类型、负责人等。
- **智能表格写入**：Agent **无权**代用户改表；PR 合并后由**仓库管理员**自行同步表格。

## 相关 Skill

- 端到端：查负责人、改 v2 配置与 README、提 PR → 使用 skill `max-config-center-pr`（`skills/max-config-center-pr/SKILL.md`）。
